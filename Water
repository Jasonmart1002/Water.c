#include <stdio.h>
#include <cs50.h>

int main (void)
{
    printf("minutes: ");
    
    int minutes = get_int();
    
    if(minutes > 0)
    {
        printf("botlles: %i\n", minutes * 12);
    }
    
    else
    {
        printf("must be a positive number.\n");
    }
}
